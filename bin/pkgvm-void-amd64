#!/bin/sh

main() {
	QEMU_ARCH=x86_64
	QEMU_DISK=void-amd64.qcow2
	QEMU_PORT=8888
	QEMU_CPUS=1
	QEMU_RAMS=1536
	QEMU_ENET=02:d3:e1:b9:5b:0a

	exec qemu-system-${QEMU_ARCH} \
		-machine type=q35,accel=hvf \
		-cpu host \
		-smp ${QEMU_CPUS} \
		-m ${QEMU_RAMS} \
		-nic user,mac=${QEMU_ENET},model=virtio,hostfwd=tcp::${QEMU_PORT}-:22,ipv6=off \
		-drive file=${QEMU_DISK},if=virtio,format=qcow2,index=0 \
		-nographic \
		-serial mon:stdio \
		"$@"
}


cd "$(dirname "$(realpath "$0")")"/../var/disks
main "$@"
exit $?
