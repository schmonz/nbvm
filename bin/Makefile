pkgbuild:
	mkdir -p $${HOME}/bin
	ln -sf `pwd`/pkgbuild-* `pwd`/qemu-* `pwd`/pkgvm-* $${HOME}/bin
	ln -sf $${HOME}/bin/pkgbuild-make $${HOME}/bin/make
	ln -sf /opt/pkg/bin/cvs-for-gits $${HOME}/bin/cvs
